<header class="">
<nav class="navbar navbar-expand-lg navbar-light bg-light mx-4">
  <a class="navbar-brand" href="{{ path("home_index") }}">LocaVoit</a>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <div class="collapse navbar-collapse" id="navbarNav">
    <ul class="navbar-nav">
     <li class="nav-item active"><a class="nav-link" href="{{ path("home_index") }}">Accueil</a></li>
     <li class="nav-item"><a class="nav-link" href="{{ path("home_end") }}">Commander</a></li>
    {% if app.user is null %}
        <li class="nav-item"><a class="nav-link" href="{{ path("home_rent") }}">Connexion/Inscription</a></li>
    {% endif %}
    <li class="nav-item"><a class="nav-link" href="{{ path("app_logout") }}">Déconnexion</a></li>
    {% if is_granted("ROLE_ADMIN") %}
      <li class="nav-item"><a class="nav-link" href="{{ path("vehicule_list") }}">Back Office</a></li>
    {% endif %}
  </div>
</nav>
</header>
